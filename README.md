# Twitter-X-Keyword-Fetcher
A simple **Python script** that fetches recent posts from **X (formerly Twitter)** based on specific keywords using the official **X API v2**.   The script saves tweets — including author name, username, date, and text — into a local `tweets.txt` file.  This project is built **for learning purposes**
